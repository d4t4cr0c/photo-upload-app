import React from 'react';
import { Container } from '@/components/Container';
import { UIShowcase } from '@/components/UIShowcase';

export const StyleTestScreen: React.FC = () => {
  return (
    <Container>
      <UIShowcase />
    </Container>
  );
};